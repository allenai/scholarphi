 This module implements a detector for definitions in LaTeX. It requires several
 NLP and ML packages to function, which are included in the `requirements.txt`
 file in the `data-processing` directory.
